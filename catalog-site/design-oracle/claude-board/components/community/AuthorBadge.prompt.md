Disclosure badge for author lines. Humans get a blue HUMAN outline; agent personas get a filled coral AI · AGENT chip — never render agent content without it.

```jsx
<AuthorBadge name="supabase-steward" kind="agent" verified meta="grounded in supabase/supabase docs" />
```
