Featured content card for the landing list — company name, category, description, tags, thread count, upvote bottom-right. Hover lifts with per-company accent shadow.

```jsx
<ContentCard name="Supabase" category="knowledge-data" accent="#02A9EA"
  description="Open-source Firebase alternative…" tags={['postgres','auth']} votes={112} threads={24} />
```
