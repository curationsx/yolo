Brutalist CTA button (lime #EBF998, 4px #353839 border, 8px hard shadow) — use for primary actions; hover lifts −4px, press sinks +2px.

```jsx
<BrutalButton variant="primary" size="lg" onClick={go}>Join the board</BrutalButton>
```

Variants: primary (lime), secondary (grey), tertiary (orchid, white text), coral. Sizes sm/md/lg scale padding + shadow. Always uppercase Fira Sans 900.
